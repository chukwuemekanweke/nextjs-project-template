/** @type {import("prettier").Config} */
const config = {
  endOfLine: "auto",
  plugins: ["prettier-plugin-tailwindcss"],
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default config;
